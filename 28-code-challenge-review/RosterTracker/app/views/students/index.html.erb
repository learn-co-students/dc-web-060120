<h1>Students#index</h1>

<ul>
  <% @students.each do |student| %>
    <li>
      <%= link_to "#{student.name} - #{student.grade}", student_path(student) %>
      <%= link_to "Delete this student", student_path(student), method: "DELETE" %>
    </li>
  <% end %>
</ul>
